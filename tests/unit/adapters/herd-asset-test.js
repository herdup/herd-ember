import { moduleFor, test } from 'ember-qunit';

moduleFor('adapter:herd-asset', 'Unit | Adapter | herd asset');

test('it exists', function(assert) {
  var adapter = this.subject();
  assert.ok(adapter);
});

